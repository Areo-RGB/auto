import path from "path";

export const ROOT_DIR = path.resolve(__dirname, "..", "..");

export const REFEREE_JSON_PATH = path.join(ROOT_DIR, "referees.json");
export const REFEREE_CSV_PATH = path.join(ROOT_DIR, "referees.csv");
